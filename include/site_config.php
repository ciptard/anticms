<?php
global $config;
$config = array();

// If you host your site under a subdirectory, e.g. example.com/mysite, you can set it here:
// $config('root') = 'mysite';
// This value is used by abs_path(), which generates an absolute path:
// abs_path('directory/page') => '/mysite/directory/page'
$config['root'] = '';
?>